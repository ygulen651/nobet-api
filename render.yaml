services:
  - type: web
    name: nobet-eczane-api
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python api_server.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0

